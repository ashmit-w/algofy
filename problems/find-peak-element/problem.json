{
  "id": "find-peak-element",
  "title": "Find Peak Element",
  "description": "A peak element is one that is strictly greater than its neighbours. Given an array, return the index of any peak element. The array boundary is treated as negative infinity. If multiple peaks exist, returning any valid index is accepted.",
  "inputFormat": "A single line of space-separated integers",
  "outputFormat": "A single integer — the index of a peak element",
  "examples": [
    { "input": "1 2 3 1",         "output": "2 (only valid answer)" },
    { "input": "1 2 1 3 5 6 4",   "output": "1 or 5 (both valid)" }
  ],
  "timeoutMs": 5000,
  "hasTester": true
}
